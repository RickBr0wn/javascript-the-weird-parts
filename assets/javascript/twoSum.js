function twoSum(numbers, target) {
  let answer = new Array()
  
  return answer
}

function numericalCompare(a, b) {
  return a - b;
}

// Should return: [0, 2]
twoSum([1, 2, 3], 4).sort(numericalCompare) //?

// Should return: [1, 2]
twoSum([1234, 5678, 9012], 14690).sort(numericalCompare) //?

// Should return: [0, 1]
twoSum([2, 2, 3], 4).sort(numericalCompare) //?
